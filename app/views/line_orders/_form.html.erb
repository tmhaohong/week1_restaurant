<%= form_for(@line_order) do |f| %>
  <% if @line_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@line_order.errors.count, "error") %> prohibited this line_order from being saved:</h2>

      <ul>
      <% @line_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.number_field :order_id %>
  </div>
  <div class="field">
    <%= f.label :food_id %><br>
    <%= f.number_field :food_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
